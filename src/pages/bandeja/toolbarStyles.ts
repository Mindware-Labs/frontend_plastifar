/** Botón de icono de la barra del visor: neutro en reposo, interacción fluida al pasar. */
export const iconButtonClass =
  "flex h-7 w-7 shrink-0 items-center justify-center rounded-lg text-zinc-500 outline-none " +
  "transition-all hover:bg-zinc-100 hover:text-zinc-900 active:scale-95 focus-visible:ring-2 focus-visible:ring-brand-red/20 " +
  "disabled:cursor-not-allowed disabled:opacity-35 disabled:hover:bg-transparent";

/** Etiquetas de los editores: ancho uniforme para alineación limpia. */
export const fieldLabelClass =
  "w-12 shrink-0 font-heading text-[10.5px] font-bold uppercase tracking-[0.08em] text-zinc-400";

/** Separador vertical fino entre herramientas. */
export const dividerClass = "mx-0.5 h-4 w-px shrink-0 bg-zinc-200/80";

/** Botón pill para alternar campos de copia (CC, CCO, Todos). */
export const fieldToggleClass =
  "shrink-0 rounded-md border border-zinc-200 bg-white px-2 py-0.5 font-heading text-[10.5px] font-bold uppercase " +
  "tracking-wider text-zinc-500 shadow-2xs outline-none transition-all hover:border-zinc-300 hover:bg-zinc-50 hover:text-zinc-900 " +
  "active:scale-95 focus-visible:ring-2 focus-visible:ring-zinc-400/20 cursor-pointer";

/** Botón de cierre compacto para chips y campos de copia. */
export const fieldCloseClass =
  "flex h-5 w-5 shrink-0 items-center justify-center rounded-md text-zinc-400 outline-none " +
  "transition-colors hover:bg-zinc-100 hover:text-zinc-700 focus-visible:ring-2 focus-visible:ring-zinc-400/20 cursor-pointer";

